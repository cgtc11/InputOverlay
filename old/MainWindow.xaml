<Window x:Class="InputOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="InputOverlay"
        Width="350" Height="280"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        MouseRightButtonDown="Window_MouseRightButtonDown"
        MouseRightButtonUp="Window_MouseRightButtonUp"
        MouseMove="Window_MouseMove">
    <Grid Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="39*"/>
            <RowDefinition Height="86*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!-- 背景ブラシを起動時から半透明に設定 -->
        <Border x:Name="OverlayBorder"
                Background="#80000000"
            CornerRadius="10"
                Padding="10" Grid.ColumnSpan="2" Height="280" Width="350" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.RowSpan="2">
            <StackPanel>
                <TextBlock x:Name="MouseText"
                           Foreground="White"
                           FontSize="48"
                           Text="🖱:□□□"
                           TextAlignment="Center"
                           Margin="0,0,0,10"/>

                <TextBlock x:Name="KeyText"
                           Foreground="White"
                           FontSize="36"
                           Text=""
                           TextAlignment="Center"
                           Margin="0,0,0,10"/>
                
                <TextBlock x:Name="PrevKeyText"
                           Foreground="White"
                           FontSize="36"
                           Text=""
                           TextAlignment="Center"
                           Opacity="0.4"
                           Margin="0,0,0,10"/>
                
                <TextBlock x:Name="PrevPrevKeyText"
                           Foreground="White"
                           FontSize="36"
                           Text=""
                           TextAlignment="Center"
                           Opacity="0.1"
                           Margin="0,0,0,10"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
